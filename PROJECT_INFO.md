# 🚀 FOMO - Крипто Аналитическая Платформа

## 📋 Описание проекта

**FOMO** - это современная fullstack платформа для криптоаналитики с красивым лендингом, построенная на:
- **Backend**: FastAPI + MongoDB (асинхронный)
- **Frontend**: React 19 + Radix UI + Tailwind CSS

## 🎯 Основные возможности

### Backend API
- ✅ FastAPI сервер с асинхронной поддержкой
- ✅ MongoDB интеграция через Motor
- ✅ CORS настроен
- ✅ API endpoints для status checks
- ✅ UUID вместо ObjectID для упрощения работы

### Frontend
- ✅ Красивый многосекционный лендинг
- ✅ Адаптивный дизайн
- ✅ Интерактивные компоненты (графики, анимации)
- ✅ Radix UI компоненты
- ✅ Tailwind CSS стилизация
- ✅ Секции: Hero, About, Roadmap, Evolution, Team, Portfolio, Partners, Community

## 📁 Структура проекта

```
/app/
├── backend/
│   ├── .env                    # Переменные окружения backend
│   ├── server.py               # Главный файл FastAPI
│   └── requirements.txt        # Python зависимости
│
├── frontend/
│   ├── .env                    # Переменные окружения frontend
│   ├── package.json            # Node.js зависимости
│   ├── craco.config.js         # Конфигурация CRACO
│   ├── tailwind.config.js      # Конфигурация Tailwind
│   ├── public/                 # Статические файлы
│   └── src/
│       ├── App.js              # Главный React компонент
│       ├── App.css             # Стили приложения
│       ├── index.js            # Точка входа
│       ├── components/         # React компоненты
│       ├── hooks/              # Custom hooks
│       └── lib/                # Утилиты
│
└── PROJECT_INFO.md             # Этот файл
```

## 🔧 Конфигурация

### Backend (.env)
```env
MONGO_URL=mongodb://localhost:27017
DB_NAME=fomo_db
CORS_ORIGINS=*
```

### Frontend (.env)
```env
REACT_APP_BACKEND_URL=http://localhost:8001
```

## 🚀 Управление сервисами

### Перезапуск сервисов
```bash
# Перезапустить backend
sudo supervisorctl restart backend

# Перезапустить frontend
sudo supervisorctl restart frontend

# Перезапустить оба
sudo supervisorctl restart all
```

### Проверка статуса
```bash
sudo supervisorctl status
```

### Просмотр логов
```bash
# Backend логи
tail -f /var/log/supervisor/backend.out.log
tail -f /var/log/supervisor/backend.err.log

# Frontend логи
tail -f /var/log/supervisor/frontend.out.log
tail -f /var/log/supervisor/frontend.err.log
```

## 🌐 API Endpoints

### Backend API (порт 8001)

#### GET /api/
Проверка работоспособности API
```bash
curl http://localhost:8001/api/
```
Ответ:
```json
{
  "message": "Hello World"
}
```

#### POST /api/status
Создать новый status check
```bash
curl -X POST http://localhost:8001/api/status \
  -H "Content-Type: application/json" \
  -d '{"client_name": "Test Client"}'
```
Ответ:
```json
{
  "id": "uuid-string",
  "client_name": "Test Client",
  "timestamp": "2025-12-24T12:00:00Z"
}
```

#### GET /api/status
Получить все status checks
```bash
curl http://localhost:8001/api/status
```
Ответ:
```json
[
  {
    "id": "uuid-string",
    "client_name": "Test Client",
    "timestamp": "2025-12-24T12:00:00Z"
  }
]
```

## 💻 Разработка

### Backend

#### Установка зависимостей
```bash
cd /app/backend
pip install -r requirements.txt
```

#### Добавление новых зависимостей
```bash
cd /app/backend
pip install <package-name>
pip freeze > requirements.txt
sudo supervisorctl restart backend
```

### Frontend

#### Установка зависимостей
```bash
cd /app/frontend
yarn install
```

#### Добавление новых пакетов
```bash
cd /app/frontend
yarn add <package-name>
# Автоматически обновит package.json
```

## 🎨 Frontend секции

1. **Navigation** - Навигационное меню с адаптивным дизайном
2. **Hero Section** - Главная секция с интерактивным графиком
3. **About Section** - О платформе с feature cards
4. **Roadmap Section** - Дорожная карта проекта с фильтрами
5. **Evolution Section** - Система уровней пользователей
6. **Team Section** - Команда проекта
7. **Portfolio Section** - Портфолио проектов
8. **Partners Section** - Партнеры платформы
9. **Community Section** - Социальные сети
10. **Footer** - Подвал с ссылками

## 📝 Дальнейшая разработка

### Рекомендации:
1. **Добавление новых API endpoints** - редактируйте `/app/backend/server.py`
2. **Добавление новых React компонентов** - создавайте в `/app/frontend/src/components/`
3. **Изменение стилей** - редактируйте `/app/frontend/src/App.css`
4. **Настройка Tailwind** - конфигурируйте `/app/frontend/tailwind.config.js`

### Hot Reload включен:
- Backend автоматически перезагружается при изменениях в `server.py`
- Frontend автоматически перезагружается при изменениях в `src/`

### Когда нужно перезапускать сервисы:
- ✅ При установке новых Python пакетов
- ✅ При установке новых npm/yarn пакетов
- ✅ При изменении .env файлов
- ❌ НЕ нужно при изменении кода (hot reload работает)

## 🔍 Полезные команды

```bash
# Проверить подключение к MongoDB
mongo mongodb://localhost:27017/fomo_db --eval "db.stats()"

# Посмотреть все записи в коллекции status_checks
mongo mongodb://localhost:27017/fomo_db --eval "db.status_checks.find().pretty()"

# Очистить коллекцию status_checks
mongo mongodb://localhost:27017/fomo_db --eval "db.status_checks.deleteMany({})"

# Проверить порты
netstat -tuln | grep -E ':(3000|8001|27017)'

# Проверить процессы
ps aux | grep -E '(uvicorn|node|mongod)'
```

## ✅ Статус развертывания

- ✅ Backend запущен на порту 8001
- ✅ Frontend запущен на порту 3000
- ✅ MongoDB работает на порту 27017
- ✅ API тестирован и работает
- ✅ Все зависимости установлены
- ✅ Hot reload настроен
- ✅ Supervisor настроен для автозапуска

## 🎉 Готово к разработке!

Проект полностью развернут и готов к дальнейшей доработке. Вы можете:
- Добавлять новые features
- Менять дизайн
- Добавлять новые API endpoints
- Интегрировать дополнительные сервисы

Все изменения будут автоматически применяться благодаря hot reload!
