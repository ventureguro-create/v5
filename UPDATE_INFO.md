# 🎉 Обновление FOMO Platform - Декабрь 2024

## 📦 Что нового в этой версии

### ✨ Новые функции

#### 1. **Project Drawer Component** - 3D карточки проектов
- 🎴 Интерактивный 3D просмотр карточек проектов
- 🖱️ Эффект "веера" при наведении мышью - как будто листаете карточки пальцем
- 🌟 Плавные анимации и трансформации
- 📱 Адаптивный дизайн для разных устройств
- 🎨 Красивые визуальные эффекты (тени, градиенты, глубина)

**Как это работает:**
- При наведении на секцию проектов карточки раскрываются веером
- Движение мыши влево/вправо переключает фокус между карточками
- Клик по карточке открывает ссылку на проект
- В неактивном состоянии карточки сложены стопкой

#### 2. **API управления карточками проектов**

Новые endpoints для полного CRUD управления карточками:

**GET /api/drawer-cards** - Получить все карточки
```bash
curl http://localhost:8001/api/drawer-cards
```

**POST /api/drawer-cards** - Создать новую карточку
```bash
curl -X POST http://localhost:8001/api/drawer-cards \
  -H "Content-Type: application/json" \
  -d '{
    "title": "My Project",
    "link": "https://myproject.com",
    "image_url": "https://example.com/image.jpg",
    "order": 1
  }'
```

**PUT /api/drawer-cards/{card_id}** - Обновить карточку
```bash
curl -X PUT http://localhost:8001/api/drawer-cards/UUID \
  -H "Content-Type: application/json" \
  -d '{"title": "Updated Title"}'
```

**DELETE /api/drawer-cards/{card_id}** - Удалить карточку
```bash
curl -X DELETE http://localhost:8001/api/drawer-cards/UUID
```

**POST /api/drawer-cards/reorder** - Изменить порядок карточек
```bash
curl -X POST http://localhost:8001/api/drawer-cards/reorder \
  -H "Content-Type: application/json" \
  -d '[{"id": "uuid1", "order": 1}, {"id": "uuid2", "order": 2}]'
```

#### 3. **API загрузки изображений**

**POST /api/upload-image** - Загрузить изображение
```bash
curl -X POST http://localhost:8001/api/upload-image \
  -F "file=@image.png"
```

**Поддерживаемые форматы:**
- PNG ✅ (рекомендуется)
- JPEG ✅
- WebP ✅ (рекомендуется)
- JPG ✅

**Рекомендуемые параметры изображений:**
- Размер: 1200x800px (соотношение 3:2)
- Формат: PNG или WebP для лучшего качества
- Максимальный размер файла: 5MB

### 🔧 Технические улучшения

1. **Директория uploads**
   - Создана `/app/backend/uploads/` для хранения загруженных изображений
   - Статические файлы доступны по URL `/uploads/{filename}`

2. **Модели данных**
   - Добавлены Pydantic модели для DrawerCard
   - Поддержка валидации данных
   - Автоматические timestamps (created_at, updated_at)

3. **UI компоненты**
   - ProjectDrawer с 3D трансформациями
   - Оптимизированные CSS анимации
   - Адаптивный дизайн

### 📊 Обновленная структура данных

**DrawerCard модель:**
```json
{
  "id": "uuid-string",
  "title": "Project Title",
  "link": "https://project-url.com",
  "image_url": "https://image-url.com/image.jpg",
  "order": 1,
  "created_at": "2025-12-24T12:00:00Z",
  "updated_at": "2025-12-24T12:00:00Z"
}
```

## 🚀 Как использовать новые функции

### 1. Добавление проектов через API

```bash
# Создать новую карточку проекта
curl -X POST http://localhost:8001/api/drawer-cards \
  -H "Content-Type: application/json" \
  -d '{
    "title": "CryptoVault",
    "link": "https://cryptovault.io",
    "image_url": "https://images.unsplash.com/photo-xyz",
    "order": 1
  }'
```

### 2. Загрузка собственного изображения

```bash
# Загрузить изображение
curl -X POST http://localhost:8001/api/upload-image \
  -F "file=@/path/to/image.png"

# Ответ содержит URL изображения
{
  "filename": "uuid.png",
  "url": "/uploads/uuid.png",
  "message": "Image uploaded successfully"
}

# Используйте полный URL для карточки
# http://localhost:8001/uploads/uuid.png
```

### 3. Управление карточками из кода

В `/app/frontend/src/App.js` компонент ProjectDrawer автоматически загружает карточки:

```javascript
// Загрузка карточек
useEffect(() => {
  const fetchCards = async () => {
    const response = await axios.get(`${API}/drawer-cards`);
    setCards(response.data);
  };
  fetchCards();
}, []);
```

## 📝 Миграция данных

Если у вас уже были проекты, вам нужно:

1. **Создать карточки для существующих проектов:**
```bash
# Для каждого проекта
curl -X POST http://localhost:8001/api/drawer-cards \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Ваш проект",
    "link": "https://ссылка",
    "image_url": "https://изображение",
    "order": 1
  }'
```

2. **Или добавить через интерфейс** (если будет админ панель)

## 🎨 Кастомизация внешнего вида

### Изменение эффекта веера

В `/app/frontend/src/App.js`, функция `getCardStyle`:

```javascript
// Настройте параметры
const stackOffset = 6;  // Расстояние между картами в стопке
const fanSpread = 140;  // Горизонтальное расстояние при раскрытии
```

### Изменение анимаций

В `/app/frontend/src/App.css`:

```css
.drawer-card {
  transition: all 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
  /* Измените продолжительность и easing */
}
```

## 🔍 Тестирование

### 1. Проверка API
```bash
# Список всех карточек
curl http://localhost:8001/api/drawer-cards

# API документация
# Откройте в браузере: http://localhost:8001/docs
```

### 2. Проверка Frontend
- Откройте http://localhost:3000
- Прокрутите до секции "Our Projects"
- Наведите мышь на карточки
- Проверьте эффект веера

### 3. Проверка загрузки изображений
```bash
# Создайте тестовое изображение
curl -X POST http://localhost:8001/api/upload-image \
  -F "file=@test.png"

# Проверьте что файл доступен
curl -I http://localhost:8001/uploads/[filename]
```

## 📚 Дополнительные ресурсы

### Полезные ссылки для изображений:
- **Unsplash**: https://unsplash.com/ (бесплатные высококачественные фото)
- **Pexels**: https://www.pexels.com/ (бесплатные стоковые фото)

### Рекомендуемые размеры изображений:
```
Desktop:  1200x800px (3:2)
Tablet:   900x600px (3:2)
Mobile:   600x400px (3:2)
```

### Оптимизация изображений:
```bash
# Используйте WebP для лучшего сжатия
# TinyPNG для оптимизации PNG/JPEG
# Или онлайн инструменты:
# https://tinypng.com/
# https://squoosh.app/
```

## 🐛 Известные проблемы

1. **Производительность с большим количеством карточек**
   - Рекомендуется не более 10-15 карточек для оптимальной производительности
   - При большем количестве можно добавить пагинацию

2. **Мобильные устройства**
   - На мобильных эффект веера заменен на свайп
   - Тестирование touch событий в процессе

## 📊 Текущий статус

✅ Backend API полностью работает
✅ Frontend компонент реализован
✅ Загрузка изображений работает
✅ 3D эффекты работают
✅ База данных настроена
✅ 5 тестовых карточек добавлены

## 🎯 Следующие шаги

1. ✨ Добавить админ панель для управления карточками
2. 🎨 Добавить больше эффектов перехода
3. 📱 Улучшить мобильную версию
4. 🔍 Добавить поиск и фильтрацию проектов
5. 📊 Добавить аналитику (клики, просмотры)

## 💡 Советы по использованию

1. **Качественные изображения** - используйте высококачественные изображения для лучшего впечатления
2. **Консистентность** - старайтесь использовать изображения одного стиля
3. **Порядок** - располагайте важные проекты первыми (меньший order)
4. **Ссылки** - убедитесь что все ссылки рабочие
5. **Тестирование** - проверяйте на разных устройствах

## 🆘 Помощь

Если возникли проблемы:

1. Проверьте логи:
```bash
tail -f /var/log/supervisor/backend.err.log
tail -f /var/log/supervisor/frontend.out.log
```

2. Проверьте API:
```bash
curl http://localhost:8001/api/drawer-cards
```

3. Перезапустите сервисы:
```bash
sudo supervisorctl restart all
```

---

**Версия:** 1.1.0 (Декабрь 2024)
**Совместимость:** FOMO Platform v1.0+
