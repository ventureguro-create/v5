# 🎴 Руководство по работе с Project Drawer

## Что это?

**Project Drawer** - это интерактивный 3D компонент для отображения карточек проектов. При наведении мыши карточки раскрываются веером, как колода карт, создавая эффектный визуальный эффект.

## 🎯 Быстрый старт

### 1. Просмотр карточек

Откройте http://localhost:3000 и прокрутите до секции "Our Projects"

**Как взаимодействовать:**
- 🖱️ Наведите мышь на область с карточками
- ⬅️➡️ Двигайте мышь влево/вправо для переключения между карточками
- 👆 Кликните на карточку чтобы перейти по ссылке
- 📱 На мобильных - свайп влево/вправо

### 2. Добавление новой карточки

**Через API:**
```bash
curl -X POST http://localhost:8001/api/drawer-cards \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Мой проект",
    "link": "https://myproject.com",
    "image_url": "https://images.unsplash.com/photo-xyz",
    "order": 1
  }'
```

**Параметры:**
- `title` (обязательно) - Название проекта
- `link` (обязательно) - URL проекта
- `image_url` (обязательно) - URL изображения (или путь к загруженному)
- `order` (необязательно) - Порядок отображения (меньше = раньше)

### 3. Загрузка собственного изображения

**Шаг 1: Загрузите изображение**
```bash
curl -X POST http://localhost:8001/api/upload-image \
  -F "file=@/path/to/your/image.png"
```

**Ответ:**
```json
{
  "filename": "abc123.png",
  "url": "/uploads/abc123.png",
  "message": "Image uploaded successfully"
}
```

**Шаг 2: Создайте карточку с этим изображением**
```bash
curl -X POST http://localhost:8001/api/drawer-cards \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Мой проект",
    "link": "https://myproject.com",
    "image_url": "http://localhost:8001/uploads/abc123.png",
    "order": 1
  }'
```

## 📊 Управление карточками

### Получить все карточки
```bash
curl http://localhost:8001/api/drawer-cards
```

### Получить одну карточку
```bash
curl http://localhost:8001/api/drawer-cards/{card_id}
```

### Обновить карточку
```bash
curl -X PUT http://localhost:8001/api/drawer-cards/{card_id} \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Новое название"
  }'
```

### Удалить карточку
```bash
curl -X DELETE http://localhost:8001/api/drawer-cards/{card_id}
```

### Изменить порядок карточек
```bash
curl -X POST http://localhost:8001/api/drawer-cards/reorder \
  -H "Content-Type: application/json" \
  -d '[
    {"id": "uuid1", "order": 1},
    {"id": "uuid2", "order": 2},
    {"id": "uuid3", "order": 3}
  ]'
```

## 🎨 Рекомендации по изображениям

### Размеры
- **Оптимально:** 1200x800px (соотношение 3:2)
- **Минимум:** 800x533px
- **Максимум:** 2400x1600px

### Форматы
- ✅ **PNG** - лучшее качество
- ✅ **WebP** - лучшее сжатие
- ✅ **JPEG** - хороший баланс
- ❌ **GIF** - не рекомендуется

### Где взять изображения?
1. **Unsplash** - https://unsplash.com/
   ```
   https://images.unsplash.com/photo-ID?w=1200&h=800&fit=crop
   ```

2. **Pexels** - https://www.pexels.com/

3. **Собственные изображения** - загрузите через API

### Оптимизация
- Используйте сжатие (TinyPNG, Squoosh)
- Избегайте файлов > 1MB
- Используйте WebP когда возможно

## 💡 Примеры использования

### Пример 1: Крипто проект
```bash
curl -X POST http://localhost:8001/api/drawer-cards \
  -H "Content-Type: application/json" \
  -d '{
    "title": "CryptoVault Exchange",
    "link": "https://cryptovault.io",
    "image_url": "https://images.unsplash.com/photo-1639762681485-074b7f938ba0?w=1200&h=800&fit=crop",
    "order": 1
  }'
```

### Пример 2: NFT коллекция
```bash
curl -X POST http://localhost:8001/api/drawer-cards \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Pixel Punks NFT",
    "link": "https://pixelpunks.io",
    "image_url": "https://images.unsplash.com/photo-1620321023374-d1a68fbc720d?w=1200&h=800&fit=crop",
    "order": 2
  }'
```

### Пример 3: DeFi платформа
```bash
curl -X POST http://localhost:8001/api/drawer-cards \
  -H "Content-Type: application/json" \
  -d '{
    "title": "YieldFarm Pro",
    "link": "https://yieldfarm.io",
    "image_url": "https://images.unsplash.com/photo-1621761191319-c6fb62004040?w=1200&h=800&fit=crop",
    "order": 3
  }'
```

## 🔧 Кастомизация

### Изменение количества карточек
Frontend автоматически адаптируется к любому количеству карточек (рекомендуется 5-10 для оптимальной производительности)

### Изменение эффектов
Редактируйте `/app/frontend/src/App.js`:

```javascript
// Настройки эффекта веера
const stackOffset = 6;   // Расстояние в стопке
const fanSpread = 140;   // Расстояние при раскрытии
const rotateY = distanceFromFocus * -15;  // Угол поворота
```

### Изменение анимаций
Редактируйте `/app/frontend/src/App.css`:

```css
.drawer-card {
  transition: all 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
  /* Измените время и easing */
}
```

## 📱 Мобильная версия

На мобильных устройствах:
- Свайп влево/вправо для навигации
- Тап для открытия ссылки
- Уменьшенные карточки для лучшего отображения

## 🐛 Решение проблем

### Карточки не отображаются
```bash
# Проверьте что карточки есть в базе
curl http://localhost:8001/api/drawer-cards

# Проверьте логи frontend
tail -f /var/log/supervisor/frontend.out.log
```

### Изображения не загружаются
```bash
# Проверьте доступность изображения
curl -I https://your-image-url.com

# Или для локальных
curl -I http://localhost:8001/uploads/filename.png
```

### 3D эффект не работает
- Проверьте что браузер поддерживает CSS 3D transforms
- Обновите браузер до последней версии
- Проверьте что JavaScript включен

## 📊 Мониторинг

### Проверка количества карточек
```bash
curl -s http://localhost:8001/api/drawer-cards | jq 'length'
```

### Проверка размера изображений
```bash
# Получить список всех изображений
ls -lh /app/backend/uploads/
```

### Очистка старых изображений
```bash
# Удалить неиспользуемые изображения
# (будьте осторожны!)
find /app/backend/uploads/ -type f -mtime +30 -delete
```

## 🎓 Лучшие практики

1. **Используйте качественные изображения**
   - Минимум 1200x800px
   - Четкие, не размытые
   - Соответствующие теме проекта

2. **Оптимизируйте изображения**
   - Сжимайте перед загрузкой
   - Используйте WebP для веба
   - Держите размер < 500KB

3. **Логичный порядок**
   - Важные проекты первыми
   - Схожие проекты рядом
   - Актуальные выше

4. **Консистентный стиль**
   - Все изображения в одном стиле
   - Похожие цвета/тон
   - Единый формат

5. **Тестируйте**
   - Проверяйте на разных устройствах
   - Тестируйте ссылки
   - Проверяйте производительность

## 📚 API документация

Полная документация API доступна по адресу:
**http://localhost:8001/docs**

Там вы найдете:
- Все endpoints
- Схемы данных
- Примеры запросов
- Интерактивное тестирование

## 🆘 Помощь

Если что-то не работает:

1. **Перезапустите сервисы**
   ```bash
   sudo supervisorctl restart all
   ```

2. **Проверьте логи**
   ```bash
   tail -f /var/log/supervisor/backend.err.log
   ```

3. **Проверьте API**
   ```bash
   curl http://localhost:8001/api/
   ```

4. **Откройте issue** или свяжитесь с поддержкой

---

**Удачи в создании крутого портфолио проектов! 🚀**
